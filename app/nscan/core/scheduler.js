function scheduler(NScan) {
  NScan.prototype.initSchedules = function() {

    const {url, query} = this.options

    console.log(url)
    // const schedules = this.schedules

    // this.schedules = [this.options.url]
  }
}

module.exports = scheduler
